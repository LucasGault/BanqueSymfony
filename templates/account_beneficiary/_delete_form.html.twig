<form method="post" action="{{ path('account_beneficiary_delete', {'id': account_beneficiary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ account_beneficiary.id) }}">
    <button class="btn">Delete</button>
</form>
